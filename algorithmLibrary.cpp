#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    string s=to_string(1234);
    string dest;
    dest.reserve(s.length());

    bool res;
    int resint;

    copy(s.begin(),s.end(),v.begin());

}
